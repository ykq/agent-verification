{
  "name": "agent-verification",
  "version": "0.2.0",
  "description": "Evidence receipts for agent claims: multi-viewport screenshots, accessibility snapshots, structural checks, and a JSON receipt for visual verification.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "test": "bash tests/test_capture.sh"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
